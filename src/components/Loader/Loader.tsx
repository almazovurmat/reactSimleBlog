import React from 'react';
import './preloader.css';

const Loader = () => {
    return (
        <div id="preloader">
            <div className="loader">Loading...</div>
        </div>
    );
};

export default Loader;